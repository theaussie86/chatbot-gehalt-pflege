---
description: Stage all changes and commit them to the current branch
---

1. Check the status of the repository to understand what has changed using `git status`
2. specific workflow step: // turbo
3. Stage all changes using `git add .`
4. Generate a concise and descriptive commit using the git-commit-formatter skill to create a message based on the changes.
5. Commit the changes with the generated message.
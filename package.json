{
  "name": "gehalt-pflege-monorepo",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace=@pflege/web",
    "dev:api": "npm run dev --workspace=@pflege/api",
    "build": "npm run build --workspaces"
  }
}
